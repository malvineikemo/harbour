import Layout from '../components/Layout';

const HomePage = () => {
    return (
        <Layout>
            <h1>Welcome to the Home Page</h1>
            <p>This is the main content area where Markdown content will be rendered.</p>
        </Layout>
    );
};

export default HomePage;
